# 테스트 체크리스트

## 자동화 테스트
```bash
python e2e_test.py
```
- 백엔드 로직, DB 상태 전이, 문제 무결성, 라우트 응답 검증
- 14 섹션, 71개 체크 (실패 시 상세 출력)
- 실행 후 `python init_db.py`로 기본 상태 복구 권장

## 수동 UI 점검 (자동화로 안 잡히는 부분)

### 마법사 (`/admin/init`)
- [ ] 1단계: N/G 입력 시 조당 인원 박스 실시간 갱신
- [ ] N > 130 입력 시 "재생성 자동 체크" 경고 배너
- [ ] 시드 입력 (1~999999, 최대 6자리 안내 문구)
- [ ] 난이도 셀렉트박스 (Easy / Medium · Hard disabled)
- [ ] 미루기 패널티(초), 스페어 풀 비율(%) 입력
- [ ] 2단계 아코디언: 펼침 상태가 편집 후에도 유지
- [ ] ▲▼ 인접 swap, 드롭다운 1:1 swap (조 1번 ↔ 조 N 1번 형태)
- [ ] 전체 셔플 / 조 내부 셔플 / 초기 복원 버튼
- [ ] 3단계 INIT 타이핑 시 실행 버튼 활성화
- [ ] 결과 화면: firstPlayer.txt 인라인 + 복사 + 4개 다운로드 버튼

### 관리자 대시보드 (`/admin/dashboard`)
- [ ] 헤더 뱃지: 공개/비공개 / 난이도 / 스페어 N/M / 미루기 패널티 / 시드
- [ ] 공개/비공개 토글 → 참가자 화면 즉시 반영
- [ ] firstPlayer / groupRoster / challenge_data / relay.db 4개 다운로드 버튼
- [ ] 후기 (N) 버튼 → 모달 표시
- [ ] 주자 행 마지막 활동 마커 (🟢 / "X분 전" / "미접속")
- [ ] **뒤로/교환** 버튼 → 모달:
  - 라디오 3개 (맨 뒤로 / N칸 / 교환)
  - 라디오 선택 시 해당 입력만 enable
  - 사유 입력 + 실행 → 5초 자동 새로고침에 반영

### 참가자 화면 (`/`, `/challenge`, `/success`, `/defer/result`)
- [ ] 비공개 시 모든 참가자 페이지 → 준비 중 (30초 자동 새로고침)
- [ ] 메인 페이지 상품 섹션: 조별 시상 + 개인 시상 (난이도 옵션 OFF시 개인 숨김)
- [ ] 챌린지 페이지: 미루기 카드의 노란 안내 ("교환은 사전 협의 후")
- [ ] N칸 뒤로 이동: confirm 메시지가 "비밀번호 표시" 언급
- [ ] 특정 주자 교환: 드롭다운 + confirm
- [ ] 정답 제출 → success 페이지 (다음 주자 비번 + 후기 작성)
- [ ] 미루기/교환 후 → defer_result 페이지 (다음 주자 비번 + 푸른 안내 박스)
- [ ] 비번 복사 버튼 동작

### 리더보드·로스터 (`/leaderboard`, `/roster`)
- [ ] 리더보드 조별 랭킹 + 개인 랭킹 TOP 10 (`show_individual_ranking=True` 시)
- [ ] 미루기 횟수가 있는 주자 행에 부가 정보 표시
- [ ] /roster: Knox-ID 검색 → 행 하이라이트 + 자동 스크롤

### 후기·last_seen 흐름
- [ ] success 페이지에서 후기 1회 작성 → 페이지 새로고침 시 read-only로 보임
- [ ] 관리자 대시보드 "후기 (N)" 모달에 노출

## 권장 시나리오 (전체 플로우)
1. `python e2e_test.py` → 71/71 PASS 확인
2. `python init_db.py` → DB 복구
3. `python app.py` → 서버 기동
4. 관리자 로그인 → 마법사 1회 실행 (50명/5조, Medium, 시드 4242)
5. 공개 토글 → 참가자 1번 주자로 로그인 → 정답 제출
6. 새 활성 주자(2번)로 미루기 N=3 → defer_result 확인
7. 새 활성 주자로 정답 제출 → 후기 작성
8. /roster, /leaderboard 둘러보기
9. 관리자: 뒤로/교환 모달 3가지 모드 각각 시도, Pass/Skip/Reset 시도
10. 비공개 토글 → 참가자 화면 준비 중 확인
11. `python init_db.py` → 기본 상태 복구
